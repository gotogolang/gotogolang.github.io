# Copy to .env and fill in values before running docker compose up

# Required
JWT_SECRET=change-me-to-a-random-string-at-least-32-chars
EXERCISES_HOST_PATH=/opt/goalgo/exercises

# Optional
POOL_SIZE=3
CORS_ORIGINS=https://gotogolang.github.io
